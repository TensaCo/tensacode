"""Automatically extract reward from the logging logs"""

import logging

import tensorcode

# Make this optional, not instant
# @logging.add_handler
# def parse_loss(log):
#   sentiment = extract_sentiment(log)
#   tensorcode.add_loss(sentiment)
